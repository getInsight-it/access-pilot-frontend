import { CheckCircle } from "lucide-react";

export interface StatusCardData {
  label: string;
  icon: typeof CheckCircle;
  bgColor: string;
  borderColor: string;
  iconColor: string;
  textColor: string;
}
