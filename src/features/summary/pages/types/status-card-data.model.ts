import { Users } from "lucide-react";

export interface SummaryCardData {
  title: string;
  value: number;
  icon: typeof Users;
  bgColor: string;
  iconBg: string;
  iconColor: string;
  textColor: string;
  valueColor: string;
}
